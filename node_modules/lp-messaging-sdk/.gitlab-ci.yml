# The Docker image that will be used to build your app
image: node:lts

stages:
  - deploy

pages:   # notice the removed dot at the beginning
  stage: deploy
  script:
    - echo Deployed
  artifacts:
    paths:
      - public/
