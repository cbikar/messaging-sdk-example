const {send} = require("../restRequestHelper");
const HttpMethod = require("../http-method");

async function getConversationById({accountId, token, conversationId}) {

    const path = `/v1/conversations/${conversationId}`;

    return await send({accountId, token, path, method: HttpMethod.GET});
}

module.exports = getConversationById;
