const Connection = require("./connection");
const ConnectionState = require('./connection-state');

/**
 * Represents a fake connection to UMS
 * Used to inject notifications manually
 */
class NullConnection extends Connection {

    constructor(a) {
        super(a);
        this.state = ConnectionState.NULL;
    }

}

module.exports = NullConnection;
