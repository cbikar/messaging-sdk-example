/**
 * typedef {string} OS
 */

/**
 * An enum of operating system types
 * @enum {OS}
 * @category Profile
 *
 */
const OS = {
    WINDOWS: 'WINDOWS',
    LINUX: 'LINUX',
    OSX: 'OSX',
    ANDROID: 'ANDROID',
    IOS: 'IOS',
    OTHER: 'OTHER'
}

module.exports = OS;