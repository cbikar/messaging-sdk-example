/**
 * typedef {string} Integration
 */

/**
 * An enum of integration types avaialble for ClientProfile
 * @enum {Integration}
 * @category Profile
 *
 */
const Integration = {
    WEB_SDK: 'WEB_SDK',
    MOBILE_SDK: 'MOBILE_SDK',
    BRAND_SDK: 'BRAND_SDK'
}

module.exports = Integration;