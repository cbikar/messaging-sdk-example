
// https://lpgithub.dev.lprnd.net/Architecture/api-ums/blob/master/api-ums-cm/src/main/java/com/liveperson/api/ams/cm/types/CloseReason.java

/**
 * @typedef {string} DialogCloseReason
 */

/**
 * A enum of close reason values that can be passed to dialog-change when closing a dialog
 * @enum {DialogCloseReason}
 * @category Conversation
 */
const DialogCloseReason = {
    CONSUMER: 'CONSUMER',
    AGENT: 'AGENT',
    SYSTEM: 'SYSTEM'
};

module.exports = DialogCloseReason;
