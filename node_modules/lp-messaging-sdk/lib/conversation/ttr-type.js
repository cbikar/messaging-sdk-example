/**
 * @typedef {string} TTRType
 */

/**
 * A enum of possible TTR types
 * @enum {TTRType}
 * @category Conversation
 */
const TTRType = {
    NORMAL: 'NORMAL',
    URGENT: 'URGENT',
    PRIORITIZED: 'PRIORITIZED',
    CUSTOM: 'CUSTOM'
};

module.exports = TTRType;
