/**
 * @typedef {string} ParticipantChangeType
 */

/**
 * @enum {ParticipantChangeType}
 * @category Conversation
 */

const ParticipantChangeType = {
    ADD: 'ADD',
    REMOVE: 'REMOVE',
    SUGGEST: 'SUGGEST'
};

module.exports = ParticipantChangeType;
