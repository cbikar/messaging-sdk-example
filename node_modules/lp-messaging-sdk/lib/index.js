const Connection = require('./connection/connection');
const ClientProperties = require('./profile/client-properties');
const CSDS = require('./services/csds-cache');
const TokenMaintainer = require('./connection/token-maintainer');
const ShiftStatusPoller = require('./connection/shift-status-poller');

const rest = require('./api/rest').v1;

const UserType = require('./connection/user-type');
const AgentState = require('./profile/agent-state');
const ConnectionState = require('./connection/connection-state');
const ConnectionType = require('./connection/connection-type');
const ConversationState = require('./conversation/conversation-state');
const ConversationContextType = require('./conversation/conversation-context-type');
const DialogState = require('./conversation/dialog-state');
const DialogType = require('./conversation/dialog-type');
const ParticipantRole = require('./conversation/participant-role');
const ParticipantChatState = require('./conversation/participant-chat-state');
const RingState = require('./agent-matching/ring-state');
const CsatStatus = require('./conversation/csat-status')

const DeviceFamily = require('./profile/device-family');
const Features = require('./profile/features');
const Integration = require('./profile/integration');
const OS = require('./profile/os');

module.exports = {

    // functions
    createConnection: Connection.createConnection,
    createClientProperties: ClientProperties.createClientProperties,

    // helper classes
    CSDS,
    TokenMaintainer,
    ShiftStatusPoller,

    // rest api helpers
    rest,

    // enums
    UserType,
    AgentState,
    ConnectionState,
    ConnectionType,
    ConversationState,
    ConversationContextType,
    DialogState,
    DialogType,
    ParticipantRole,
    ParticipantChatState,
    RingState,
    CsatStatus,

    // client properties enums
    DeviceFamily,
    Features,
    Integration,
    OS
};
